mesh: test-echo
description: "Simple echo mesh for E2E testing"

agents:
  - name: echo
    model: haiku
    prompt: prompt.md

entry_point: echo

routing:
  echo:
    complete:
      core: "Echo complete"
