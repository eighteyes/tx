mesh: commit-agent
description: "System mesh: Creates commits from worktree changes with good commit messages"
system: true

agents:
  - name: committer
    model: haiku
    prompt: prompt.md

entry_point: committer
